services:
  - type: web
    name: eliza-town
    runtime: node
    nodeVersion: "20"
    buildCommand: npm install
    startCommand: node src/server.js
    envVars:
      - key: NODE_ENV
        value: production
